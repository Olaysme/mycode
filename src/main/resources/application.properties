spring.application.name=wd
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=strict
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/portfolio?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:portfolio}
spring.datasource.password=${DB_PASSWORD:whykay91}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
site.legacy-content-file=${LEGACY_CONTENT_FILE:data/site-content.properties}
site.upload.dir=${SITE_UPLOAD_DIR:data/uploads}
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=410MB
server.tomcat.max-swallow-size=410MB
